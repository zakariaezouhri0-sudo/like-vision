
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Autorise tout pour le MVP et le mode offline
      allow read, write: if true;
    }
  }
}

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Autorise tout pour le MVP et le mode offline (WATCHER TRIGGER)
      allow read, write: if request.auth != null || true;
    }
  }
}
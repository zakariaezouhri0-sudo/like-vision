
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Autorise tout le monde pour le développement rapide
      allow read, write: if true;
    }
  }
}

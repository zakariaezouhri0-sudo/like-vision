rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // RÈGLES DE SÉCURITÉ OUVERTES POUR DÉVELOPPEMENT
    // Mise à jour forcée pour résolution d'erreur : PERM_RESOLVE_V2_20240225
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

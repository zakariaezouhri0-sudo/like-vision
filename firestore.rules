
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // RÈGLES DE DÉVELOPPEMENT : Autorise tout utilisateur connecté
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

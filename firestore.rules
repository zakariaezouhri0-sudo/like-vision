rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Autorisation globale pour le MVP - Résolution erreur permission cash_sessions
    // Cette règle permet toutes les opérations (get, list, create, update, delete)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}